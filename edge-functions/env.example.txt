# Craft Local Edge Functions - Environment Configuration
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control!

# ============================================================
# SUPABASE CONFIGURATION (Self-Hosted)
# ============================================================

# Your self-hosted Supabase API URL
SUPABASE_URL=https://api.craftlocal.net

# Supabase Anonymous (Public) Key
# Find in: Supabase Dashboard → Settings → API → Project API keys
# This key is safe to use in client-side code
SUPABASE_ANON_KEY=your-anon-key-here

# Supabase Service Role (Admin) Key
# ⚠️ CRITICAL: This key has FULL admin access to your database
# NEVER expose this in client-side code or commit to git
# Find in: Supabase Dashboard → Settings → API → Project API keys
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# ============================================================
# SERVER CONFIGURATION
# ============================================================

# Server port (default: 8000)
PORT=8000

# Deno cache directory
DENO_DIR=/app/.deno_cache

# ============================================================
# STRIPE CONFIGURATION
# ============================================================

# Stripe Secret Key (starts with sk_live_ or sk_test_)
STRIPE_SECRET_KEY=sk_test_your-stripe-key

# Stripe Webhook Secret (for stripe-webhook function)
STRIPE_WEBHOOK_SECRET=whsec_your-webhook-secret

# ============================================================
# OPENAI CONFIGURATION (For AI features)
# ============================================================

# OpenAI API Key (for AI content generation)
OPENAI_API_KEY=sk-your-openai-key

# ============================================================
# EMAIL CONFIGURATION
# ============================================================

# For email sending functions
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-email@example.com
SMTP_PASSWORD=your-smtp-password
EMAIL_FROM=noreply@craftlocal.net

# ============================================================
# EXTERNAL SERVICES
# ============================================================

# Google Search Console (for SEO functions)
GSC_CLIENT_ID=your-gsc-client-id
GSC_CLIENT_SECRET=your-gsc-client-secret

# Shipping Provider (for shipping label functions)
SHIPPO_API_KEY=your-shippo-api-key

# ============================================================
# NOTES
# ============================================================
# 
# Security Best Practices:
# 1. Never commit this file to version control
# 2. Use different keys for development/staging/production
# 3. Rotate keys regularly
# 4. Use secrets management in production (Docker secrets, K8s secrets)
# 5. Limit service role key usage to server-side functions only
#
# Production Deployment:
# - For Docker: Create .env file with production values
# - For Coolify: Set environment variables in dashboard
# - For K8s: Use kubectl create secret
#
