# Copilot Instructions

Never include real API keys, secrets, or credentials in generated code.
Always use environment variables for sensitive data.
Reference Cloudflare Pages env vars and self-hosted Supabase credentials from environment only.
Generate .env.example files with placeholder values when env vars are needed.
